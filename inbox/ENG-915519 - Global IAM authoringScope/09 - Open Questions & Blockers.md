---
tags:
  - eng-915519
  - blockers
  - open-questions
updated: 2026-05-28
---

# Open Questions & Blockers

What's still in the way of merging the three PRs, organized by who needs to act.

## Truly blocked on external decisions

### go-swagger version pin (Manish — iam-utils PR #357)

The wrong version of `go-swagger` strips the canonical `// This file was generated by the swagger tool.` header AND drops `omitempty` from optional fields.

- Tried `v0.30.0` → 20 noisy files
- Tried `@latest` (`v0.33.2`) → 42 noisy files
- Triangulation suggests `v0.31.x` or `v0.32.x`
- User has vetoed unilateral guessing

**Need exact version from Manish.**

Affects comments PR #357 #9, #11, #12 in [[08 - PR Reviews Rollup]].

### Manish blessing of the array stance (recommended, not strictly required)

Four reviewers (Aditya x2, Praveen x2) pushed back asking for string. The provisional decision is to stay as array. To close the review threads cleanly, one of:

- Manish publicly blesses "stays as array"
- Manish acquiesces by approving the PR with the array shape intact
- Manish contests and forces a flip — which cascades into schema, GIN→b-tree, and Go type changes across all three repos

See [[10 - Decisions Log]] for the working assumption and its consequences.

## Soft-open — product decisions

### Existing-row backfill on first deploy

The migration leaves existing global rows with `authoring_scope = '{}'`. Day 1 query `$filter=authoringScope eq 'PC'` returns no matches for pre-migration data.

| Path | Behavior |
|---|---|
| **No backfill (current PR)** | Simpler. Old rows fill in gradually via normal API touches. |
| **Cluster-aware backfill** | Day 1 consistency. Migration becomes cluster-aware (Go code, not pure SQL). |

Worth checking precedent: how was this handled for `domain_uuids` when added? For in-flight `products` (ENG-932537)?

See [[06 - Storage & Migration]] for the backfill SQL sketch.

### SonarQube Quality Gate on PR #1601 (T12)

CI shows FAILED with empty metric values. Likely candidates:

- Scanner couldn't compute coverage (test files not picked up by the scanner config)
- Threshold misconfiguration
- Branch analysis config not pointing at the right SonarQube project

Needs a look at the SonarQube dashboard for PR #1601. Likely SRE / SonarQube admin territory. May not be a hard merge gate depending on org policy.

## Now-actionable (no external dependency)

Small mechanical work unblocked by the array decision:

- **PR #910 #6 / #7:** add `authoringScope` to `x-filterable-properties` in `access_policy.yaml` and `roles.yaml`
- **PR #357 #10:** set `minItems: 0, maxItems: 5` in `themis.yaml` (will land alongside the regen when version unblocks)
- **PR #1601 follow-up:** add `"authoringScope"` to `V4AccessPolicySupportedFilterFields` and `V4RoleSupportedFilterFields` in `services/server/storage/util.go` to match the schema-side filterability

## GitHub-side bookkeeping (no code changes)

- PR #910: resolve #1 and #3 as fixed; reply to #2 / #4 / #5 citing array decision
- PR #357: reply to #13 about generated headers
- PR #1601: resolve T7 / T9 / T11 as fixed in `e7ad49866`; reply to T5 / T6 / T8 / T10 citing array decision; resolve T3 / T13 as noise

## Held out (per "absolutely certain only" rule)

- PR #1601 T4 — immutability doc comment (bot wording may not match user voice; current implicit immutability via UPDATE-omit might be sufficient)
- PR #1601 T2 — invalid product type test (user said optional)
- PR #1601 T1 — bot AI test suggestion (low value, covered by NC analogue)

## See also

- [[10 - Decisions Log]] for what's been decided
- [[08 - PR Reviews Rollup]] for the comment-level table
- Up: [[00 - Index]]
